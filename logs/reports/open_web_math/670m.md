# Epiplexity Report: `open_web_math`

## ⚠️ Warnings
- 2798 steps have loss BELOW final training loss (2.1065); the model may still be improving.

## Inputs

| Parameter | Value |
|---|---|
| Model parameters | 670,000,000 |
| Total training tokens | 5,200,000,000 |
| Total training FLOPs | 2.09e+19 FLOPs |
| Loss-curve points | 10,000 |
| Initial training loss | 11.0660 nats/token |
| Final training loss | 2.1065 nats/token |
| Final validation loss | 2.1123 nats/token |

## Epiplexity Decomposition

| Quantity | Symbol | Per-token (bits) | Total |
|---|---|---|---|
| **Epiplexity** (structural info) | $S_T$ | **0.5760** | 3.00e+09 bits |
| **Time-bounded entropy** (random info) | $H_T$ | **3.0474** | depends on test-set size |
| **Total information** | $S_T + H_T$ | **3.6234** | — |

* **Epiplexity $S_T$** — measures the structural, learnable information the model absorbed from the training data.
* **Time-bounded entropy $H_T$** — the remaining per-token unpredictability in the test data.
* **$S_T + H_T$** — together they sum to the total time-bounded information content.
* **High $S_T / (S_T+H_T)$ ratio** — means the data contains rich learnable structure beyond surface statistics.

## Derived Ratios

| Metric | Value |
|---|---|
| Structural fraction $S_T / (S_T+H_T)$ | **15.90%** |
| Language / data entropy floor | 1.7000 nats (2.4526 bits) |
| $H_T$ as fraction of entropy floor | 124.25% |
| Gap to entropy floor | 0.4123 nats (0.5948 bits) |
| $S_T$ per 1B training tokens | 575,987,117 bits |

* **Structural fraction** — the proportion of total information that is learnable structure (vs. irreducible randomness).
* **Language / data entropy floor** — the estimated Shannon entropy of the data source — the minimum achievable loss.
* **$H_T$ as fraction of entropy floor** — shows how close the model is to this minimum: 100% means the model has reached the entropy floor; values above 100% mean there is still room to improve.
* **$S_T$ per 1B tokens** — normalizes epiplexity for cross-dataset comparison.

## Reference Metrics

| Metric | Value |
|---|---|
| Mean gzip complexity | 0.4632 |

* **Mean gzip complexity** (compressed / original bytes, averaged per-sample) — approximates Kolmogorov complexity.  Used together with epiplexity to disambiguate the high-structural-fraction region: trivially compressible patterns (mean gzip ≈ 0, like periodic sequences) score high on structural fraction but contain no transferable structure; genuinely rich data (natural language, chess) has both high structural fraction AND moderate-to-high mean gzip complexity.
* **Oracle next-token loss** — the irreducible entropy of the generating process — only knowable for synthetic patterns.

## Interpretation

**Moderate mean gzip complexity** (0.4632) — the data has a mix of compressible and incompressible content.
Structural fraction is high (15.90%) — substantial learnable structure was extracted (S_T = 0.5760 bits/token, H_T = 3.0474 bits/token).  The data contains both incompressible content and rich structure.
